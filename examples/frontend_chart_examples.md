Recharts and Chart.js example snippets for the frontend.

See frontend/package-scripts.md for installation commands and full TSX examples.

- Recharts: use a `ResponsiveContainer` and `LineChart` for responsive charts.
- Chart.js: use `react-chartjs-2` and register required controllers/scales before rendering.
